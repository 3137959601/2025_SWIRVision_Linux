# 400W Qt Linux/RK3588迁移执行日志

## 一、记录目的

本文档记录迁移过程中实际执行的查询、下载、安装、文件同步、源码修改、构建、测试和调试操作，服务于以下目标：

1. 任何结论都能追溯到实际命令和输出证据。
2. 环境损坏或虚拟机重装后，可以按记录重新搭建。
3. 区分“只读查询”“文件修改”“软件安装”“系统级变更”和“失败尝试”。
4. 便于后续复习Linux、Git、Qt构建、动态链接、USB和RK3588部署知识。

记录不得包含密码、私钥或其他敏感凭据。公钥指纹、软件版本和公开仓库地址可以记录。

## 二、单条记录格式

每项操作至少包含：

- 编号与日期；
- 目标环境；
- 操作类型；
- 执行原因；
- 关键命令；
- 输出与证据；
- 文件或系统变化；
- 结论与下一步。

其中“关键命令”必须记录实际执行过的命令，而不是只记录计划。若一次操作包含大量重复查询，可以保留能够复现结论的代表命令；失败命令也必须记录其退出状态和错误摘要。

## 三、已执行操作

### OP-001：读取项目目标并确认执行边界

- 日期：2026-08-16
- 环境：Windows主机
- 类型：只读查询
- 原因：开始迁移前必须确认源码基线、执行顺序、权限边界和验收要求。
- 读取文件：`C:\Users\31379\.codex\attachments\72c0afb1-cddc-4fcd-a614-5a5cd1d39567\goal-objective.md`
- 关键结论：
  - 先完成400W工程，再进行RK3588和EVS迁移。
  - 顺序为源码审计、x86_64 Linux闭环、平台抽象、RK3588部署、真机与性能验证。
  - 不覆盖Windows可用版本，不重刷板卡，不用离线测试冒充真机成功。
- 文件变化：无。

### OP-002：审计并修复Linux独立仓库的Git继承关系

- 日期：2026-08-16
- 环境：Windows主机
- 类型：Git查询与本地仓库修改
- 原因：Linux项目需要独立目录，但必须延续Windows基线历史，才能正常比较修改。
- 仓库：`D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux`
- 关键查询：

```powershell
git status --short --branch
git remote -v
git log --oneline --decorate --graph --all
git merge-base main originlab/master
git diff --stat originlab/master main
```

- 修复前证据：
  - Linux根提交为`90ce072`，没有父提交。
  - `git merge-base main originlab/master`无输出，说明没有共同祖先。
  - 远端`originlab`仍指向原Windows仓库。
- 修复方法：
  - 将旧提交保留为`backup/pre-history-fix-20260816`。
  - 使用旧提交完全相同的Git树对象创建新提交，并将Windows基线`d4c0131`设为父提交。
  - 不重新复制或改写源码文件。
- 修复后证据：
  - Linux基线提交：`0daec20`。
  - 直接父提交：`d4c0131`。
  - 共同祖先：`d4c0131ad3c1c81ad9090396efb223befc278dd8`。
  - 修复前后树哈希均为`0df88a8c93fcbbe49c3f7787097bdcead354c5f6`。
- 文件变化：只改变Git提交关系；源码树内容未变化。
- 远端变化：无推送，原远端分支未修改。

### OP-003：RK3588系统、显示、Qt、OpenGL和USB只读盘点

- 日期：2026-08-16
- 环境：RK3588，SSH别名`tl3588`
- 类型：只读查询
- 原因：不能根据驱动名称假定板端Qt、显示设备或USB环境，需要读取真实系统信息。
- 代表命令：

```bash
uname -a
cat /etc/os-release
qmake -v
ls -la /sys/class/drm
grep -H . /sys/class/drm/*/status
DISPLAY=:0 xrandr --current
glxinfo -B
lsusb
lsusb -t
pkg-config --modversion opencv4
ldconfig -p | grep -E 'Qt|libusb'
```

- 证据摘要：
  - Debian 11、AArch64、Linux`5.10.160-rt77-gfbf758505158`。
  - 8核、约7.7 GiB内存、根分区约6.7 GiB可用。
  - Xorg、LightDM和Xfce正在运行。
  - DRM报告DSI-1和DSI-2为`connected`，但用户确认没有物理DSI屏；因此该状态只能视为设备树/驱动逻辑状态。
  - 输入子系统注册ADS7846，`lsusb`曾报告Logitech接收器，但用户确认没有现场连接相应设备；不能据此推断实物存在。
  - HDMI、DP报告为`disconnected`。
  - X11 OpenGL当前为Mesa 20.3.5的`llvmpipe`软件渲染，Rockchip DRI加载失败。
  - 存在两路5 Gbit/s xHCI根端口；T630尚未枚举。
- 文件变化：无。
- 纠正事项：后续显示与输入硬件结论以实际接线为准，不能仅依据内核节点。

### OP-004：RK3588软件开发环境与依赖版本盘点

- 日期：2026-08-16
- 环境：RK3588
- 类型：只读查询
- 原因：确定Qt5/Qt6选择、原生编译可行性和缺少的开发包。
- 实际执行的关键命令：

```bash
gcc --version
g++ --version
cmake --version
command -v make ninja qmake pkg-config
PKG_CONFIG_PATH=/usr/lib/qt-5.15.8/lib/pkgconfig pkg-config --modversion Qt5Core Qt5SerialPort
find /usr/include /usr/lib/qt-5.15.8 -type f \
  \( -name QCoreApplication -o -name QSerialPort -o -name QOpenGLWidget \)
pkg-config --modversion opencv4
test -f /usr/include/libusb-1.0/libusb.h
```

- 证据摘要：
  - GCC C编译器10.2.1存在；G++、make、Ninja和qmake缺失。
  - CMake 3.18.4、pkg-config 0.29.2存在。
  - OpenCV 4.5.1开发环境可用。
  - libusb只有运行库，缺少`libusb.h`和开发包。
  - Debian Qt5.15.2运行库与厂商Qt5.15.8运行库并存。
  - 厂商Qt5.15.8的pkg-config文件仍指向厂商构建机路径`/home/tronlong/work/.../sysroots/x86_64-linux`，板端没有相应头文件和qmake，不能作为完整SDK直接使用。
- 源码兼容检查：当前明确发现的Qt6专用调用为`QMouseEvent::position()`，Qt5需要使用`localPos()`或条件编译；其他Qt5兼容问题需要以真实编译为准。
- 文件变化：无。

### OP-005：RK3588 APT安装方案只读模拟

- 日期：2026-08-16
- 环境：RK3588
- 类型：只读软件包模拟
- 原因：在实际安装前确认版本、依赖、升级范围和是否删除现有软件。
- 命令：

```bash
apt-get -s install g++ make ninja-build qt5-qmake qtbase5-dev \
  libqt5serialport5-dev libusb-1.0-0-dev libgl1-mesa-dev
```

- 模拟结果：
  - 新增17个包。
  - 将5个Debian Qt5.15.2运行库升级到同一安全补丁版本。
  - 不删除任何软件。
  - 此命令使用`-s`，没有实际安装或修改系统。
- 结论：RK3588后续可使用Debian Qt5.15.2完整开发环境；Windows继续Qt6，同一源码通过少量版本条件兼容。

### OP-006：修复Ubuntu虚拟机SSH主机身份记录

- 日期：2026-08-16
- 环境：Windows主机与Ubuntu虚拟机`192.168.1.110`
- 类型：SSH安全诊断与配置修改
- 原因：虚拟机重装后SSH主机密钥变化，严格校验拒绝连接。
- 安全过程：
  - 先读取旧ED25519/ECDSA指纹。
  - 使用`ssh-keyscan`读取当前RSA/ECDSA/ED25519公钥并计算指纹。
  - 用户确认虚拟机确实重装后，才替换`known_hosts`中该IP的两条旧记录。
- 实际执行的关键查询：

```powershell
ssh-keygen -F 192.168.1.110 -f C:\Users\31379\.ssh\known_hosts
ssh-keyscan -T 5 -t rsa,ecdsa,ed25519 192.168.1.110
```

- 实际文件修改：使用补丁精确替换`known_hosts`中`192.168.1.110`的旧RSA/ECDSA/ED25519记录，没有执行全文件清空。
- 新ED25519指纹：`SHA256:guxWFBf/HEg+ZYp1raCwHr27mydNRVEftvr1Ft1lDi4`。
- 修改文件：`C:\Users\31379\.ssh\known_hosts`。
- 其他主机记录：未修改。

### OP-007：恢复Ubuntu虚拟机SSH公钥认证

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：用户在虚拟机控制台执行一次授权
- 原因：重装后`d508`用户的`authorized_keys`丢失，SSH进入用户认证阶段后返回`Permission denied`。
- 操作：用户将现有Codex ED25519公钥加入`~/.ssh/authorized_keys`，目录权限设为700，文件权限设为600。
- 用户在虚拟机控制台实际执行的关键命令：

```bash
install -d -m 700 "$HOME/.ssh"
grep -qxF '<Codex ED25519公钥>' "$HOME/.ssh/authorized_keys" 2>/dev/null \
  || printf '%s\n' '<Codex ED25519公钥>' >> "$HOME/.ssh/authorized_keys"
chmod 600 "$HOME/.ssh/authorized_keys"
```

- 说明：日志用占位符代替完整公钥正文；公钥不是密码，但复现时应从Windows主机的`C:\Users\31379\.ssh\codex_ubuntu20_ed25519.pub`读取当前值。
- 验证结果：SSH登录恢复；主机名`d508`，用户UID 1000，属于sudo组。
- 敏感信息：未记录密码或私钥。

### OP-008：重装后Ubuntu虚拟机基线盘点

- 日期：2026-08-16
- 环境：Ubuntu虚拟机，SSH别名`ubuntu20-vm`
- 类型：只读查询
- 原因：确认重装后哪些源码和工具需要重建。
- 实际执行的关键命令：

```bash
hostname
id
uname -m
uname -r
sed -n '1,10p' /etc/os-release
df -hT / /home
command -v gcc g++ make cmake ninja qmake qmake6 pkg-config git rsync
sudo -n true
```

- 结果：
  - Ubuntu 20.04.6 LTS、x86_64、Linux 5.15.0-139-generic。
  - 根分区195 GiB，约174 GiB可用。
  - `/home/d508/projects/2025_SWIRVision_Linux`不存在。
  - GCC、G++、Git、make、CMake、Ninja、qmake均不存在。
  - Python 3.8.10、wget、tar、xz、bzip2、unzip、rsync存在。
  - SSH会话没有`DISPLAY`，属于纯终端会话。
  - `sudo -n`失败，说明sudo需要用户交互输入密码。
- 文件变化：无。

### OP-009：将Linux独立仓库复制到Ubuntu虚拟机

- 日期：2026-08-16
- 环境：Windows主机到Ubuntu虚拟机
- 类型：文件同步
- 原因：虚拟机重装后源码丢失，需要恢复完整源码和Git历史。
- 源：`D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux`
- 目标：`/home/d508/projects/2025_SWIRVision_Linux`
- 方法：使用SCP递归复制整个项目，包括`.git`目录。
- 实际执行的关键命令：

```powershell
ssh ubuntu20-vm 'mkdir -p /home/d508/projects'
scp -r D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux `
  ubuntu20-vm:/home/d508/projects/
```

- 验证：目标目录存在`.git`，远端目录大小约2.4 MiB。
- 源仓库状态：复制前`main`位于`226c87f`，工作区干净。

### OP-010：尝试下载用户级环境管理工具micromamba

- 日期：2026-08-16
- 环境：Ubuntu虚拟机与Windows主机
- 类型：下载尝试，失败
- 原因：虚拟机没有免密sudo，优先尝试在用户目录建立隔离Qt6/OpenCV/GCC环境。
- 尝试一：虚拟机从`https://micro.mamba.pm/api/micromamba/linux-64/latest`下载。
  - 结果：下载进程卡在TLS/网络连接阶段，文件保持0字节。
  - 处理：终止本次由任务启动的`wget`及其父shell，避免后台长期占用。
- 尝试二：Windows从相同地址下载。
  - 结果：Schannel TLS握手失败，没有得到有效文件。
- 尝试三：Windows从micromamba官方GitHub release直链下载。
  - 结果：同样在Schannel TLS握手阶段失败。
- 文件变化：Windows`work/`中可能留下0字节临时下载文件；没有安装软件。
- 当前判断：需要检查IPv6优先连接、镜像可达性或改用sudo安装系统构建依赖。
- 实际执行的关键命令：

```bash
wget -q --timeout=20 --tries=2 \
  -O /home/d508/.cache/codex-downloads/micromamba.tar.bz2 \
  https://micro.mamba.pm/api/micromamba/linux-64/latest
```

```powershell
curl.exe -L --fail --connect-timeout 15 --max-time 90 \
  -o work\micromamba-linux-64.tar.bz2 \
  https://micro.mamba.pm/api/micromamba/linux-64/latest

curl.exe -L --fail --connect-timeout 15 --max-time 120 \
  -o work\micromamba-linux-64 \
  https://github.com/mamba-org/micromamba-releases/releases/latest/download/micromamba-linux-64
```

### OP-011：检查Ubuntu时间、CA证书和Miniconda镜像连通性

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：只读网络诊断
- 原因：区分“证书过期”“系统时间错误”“IPv6链路异常”和“单一下载站不可达”。
- 查询结果：
  - 系统时间为2026-08-16，时区+08:00，符合当前时间。
  - `ca-certificates`已安装，证书包文件存在。
  - `wget`解析`repo.anaconda.com`后优先尝试IPv6地址，并停留在连接阶段。
- 初步结论：更像IPv6路径不可用或外部TLS链路问题，而不是本机时间和CA文件缺失。
- 下一步：在产生新的下载操作前，先记录并尝试强制IPv4；若仍失败，使用用户协助执行的sudo命令安装基础工具。
- 实际执行的关键命令：

```bash
date -Is
dpkg-query -W ca-certificates
ls -l /etc/ssl/certs/ca-certificates.crt
timeout 15 wget --spider -S \
  https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
```

### OP-012：启用专门执行日志和学习记录规则

- 日期：2026-08-16
- 环境：Windows主机
- 类型：文档规范
- 原因：用户要求所有操作形成可复现、可学习的专门记录，并详细解释原因。
- 已读取：嵌入式学习跟踪规范、求职定位、知识矩阵、复习队列、近期学习日志和当前状态。
- 使用边界：本项目是正式工程迁移，不因为AI执行过命令就把相关知识标记为用户“已掌握”；只有用户完成独立复述或检验后才更新掌握状态。
- 后续规则：每次查询、下载、安装、修改、构建和测试都先形成日志记录，再进入下一阶段；sudo操作由用户在虚拟机控制台执行，Codex提供命令、用途、影响和验证方法。

### OP-013：提交执行日志与状态文档入口

- 日期：2026-08-16
- 环境：Windows主机Linux独立仓库
- 类型：文档修改与Git提交
- 原因：执行记录必须与代码版本一起追踪，避免散落在聊天中。
- 计划检查：使用`git diff --check`检查空白符错误，只暂存`docs/迁移执行日志.md`和`docs/当前迁移状态与问题.md`。
- 计划提交说明：`建立迁移操作日志并补录执行过程`。
- 远端影响：只创建本地提交，不推送远端。

## 四、当前停止点

- Ubuntu虚拟机SSH已恢复。
- Linux独立项目已复制到虚拟机。
- 虚拟机尚未安装编译工具或Qt环境。
- 下一项操作：验证强制IPv4下载是否可行；如果不可行，给出第一组sudo基础依赖安装命令。

### OP-014：强制IPv4下载micromamba

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：网络诊断与文件下载
- 原因：前一次`wget`优先连接IPv6后停滞；强制IPv4可以验证是否为IPv6路由问题。
- 计划命令：

```bash
timeout 60 wget -4 --server-response \
  -O /home/d508/.cache/codex-downloads/micromamba.tar.bz2 \
  https://micro.mamba.pm/api/micromamba/linux-64/latest
```

- 影响范围：只覆盖此前0字节的用户缓存文件，不安装软件，不使用sudo。
- 执行结果：成功。
- 证据：
  - 强制IPv4后依次完成`micro.mamba.pm`、`api.anaconda.org`和Amazon S3重定向。
  - 下载文件大小为6,988,090字节。
  - 实际传输约7.2秒，`wget`退出码为0。
- 结论：此前阻塞主因是虚拟机优先尝试不可用的IPv6路径；后续外部下载优先显式使用IPv4。

### OP-015：安装用户级micromamba并查询精确开发包版本

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：用户目录工具安装与软件包元数据查询
- 原因：需要在无sudo条件下建立隔离的Qt6、OpenCV和编译器环境；先查询包是否存在，避免盲目创建环境。
- 计划操作：
  1. 使用`sha256sum`记录下载包摘要。
  2. 使用`tar -tjf`确认压缩包包含`bin/micromamba`。
  3. 解压到`/home/d508/.local/bin`并执行版本验证。
  4. 强制IPv4查询conda-forge中的Qt6.7.3、OpenCV4.10和GCC/G++版本。
- 影响范围：只写入`/home/d508/.local/bin`和用户级micromamba缓存，不修改系统软件。
- 实际执行的关键命令：

```bash
archive=/home/d508/.cache/codex-downloads/micromamba.tar.bz2
sha256sum "$archive"
tar -tjf "$archive" | grep '^bin/micromamba$'
mkdir -p /home/d508/.local/bin /home/d508/.local/share/mamba
tar -xjf "$archive" -C /home/d508/.local/bin \
  --strip-components=1 bin/micromamba
chmod 755 /home/d508/.local/bin/micromamba
/home/d508/.local/bin/micromamba --version
```

- 实际结果：
  - SHA-256：`8761c382127e6363bd9e0a2451aa3ef90d071a79133f736e2f759a3bf13040dd`。
  - 压缩包包含`bin/micromamba`，解压成功。
  - 执行`micromamba --version`时进程异常中止，错误为`tl::bad_expected_access<mamba::mamba_error>`。
  - 因整段脚本启用了`set -e`，Qt、OpenCV包查询没有执行。
- 文件变化：生成`/home/d508/.local/bin/micromamba`；尚未创建任何conda环境。
- 状态：失败，下一步检查ELF架构、动态库、glibc和micromamba版本兼容性。

### OP-016：诊断micromamba启动异常

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：只读二进制诊断
- 原因：区分下载损坏、架构错误、glibc不兼容和micromamba 2.9.0自身启动问题。
- 计划执行的关键命令：

```bash
file /home/d508/.local/bin/micromamba
ldd /home/d508/.local/bin/micromamba
readelf -h /home/d508/.local/bin/micromamba
getconf GNU_LIBC_VERSION
/home/d508/.local/bin/micromamba --help
```

- 影响范围：只读，不修改文件。
- 实际执行的关键命令：

```bash
file /home/d508/.local/bin/micromamba
ldd /home/d508/.local/bin/micromamba
readelf -h /home/d508/.local/bin/micromamba
getconf GNU_LIBC_VERSION
timeout 10 /home/d508/.local/bin/micromamba --help
```

- 实际结果：
  - 二进制为x86-64 ELF，解释器为`/lib64/ld-linux-x86-64.so.2`，架构正确。
  - `ldd`列出的`libc`、`libpthread`、`libm`、`libdl`等依赖均能解析。
  - 系统glibc为2.31。
  - 系统没有`readelf`，说明binutils尚未安装；这一子查询未完成。
  - `micromamba --help`仍触发`tl::bad_expected_access<mamba::mamba_error>`，退出码134。
- 结论：不是明显的下载截断、CPU架构错误或动态库缺失；当前micromamba 2.9.0不能在该虚拟机正常启动，应更换较早稳定版本。

### OP-017：查询并选择兼容Ubuntu 20.04的micromamba旧版本

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：官方包元数据下载与查询
- 原因：避免盲猜旧版本文件名，从conda-forge官方元数据中选择真实存在的Linux x86-64包。
- 计划执行的关键命令：

```bash
wget -4 -O /home/d508/.cache/codex-downloads/micromamba-metadata.json \
  https://api.anaconda.org/package/conda-forge/micromamba
python3 - <<'PY'
# 读取JSON，筛选linux-64和较早稳定版本，打印版本、文件名、下载URL和摘要
PY
```

- 影响范围：只写入用户缓存目录中的JSON元数据，不安装系统软件。
- 实际执行的关键命令：

```bash
wget -4 -q \
  -O /home/d508/.cache/codex-downloads/micromamba-metadata.json \
  https://api.anaconda.org/package/conda-forge/micromamba
sha256sum /home/d508/.cache/codex-downloads/micromamba-metadata.json
python3 -c '<读取JSON并筛选linux-64的1.5.x、2.0.x和2.3.x包>'
```

- 实际结果：
  - 元数据下载成功，大小1,487,453字节。
  - 元数据SHA-256为`994f7637b92cdd3ebbac1c72638c25d37192c707f585fd30665bc3cdb94d751d`。
  - 第一次Python筛选命令因远程引号引入意外缩进，报`IndentationError`；第二次改用单行列表推导式后成功。
  - 官方元数据确认`micromamba-1.5.12-0.tar.bz2`存在，SHA-256为`c8d0e74cdf31349b98af7a57b0faed1468c9ebb908ca95fc8fb6df2597e8a76f`。
- 选择：使用micromamba 1.5.12。原因是该稳定版本早于当前2.x系列，更适合Ubuntu 20.04/glibc 2.31；不选择alpha/rc版本。
- 状态：完成。

### OP-018：下载并安装micromamba 1.5.12

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：用户级工具下载、摘要校验和二进制替换
- 原因：替换无法启动的2.9.0，并继续创建隔离开发环境。
- 计划执行的关键命令：

```bash
wget -4 -O /home/d508/.cache/codex-downloads/micromamba-1.5.12.tar.bz2 \
  https://api.anaconda.org/download/conda-forge/micromamba/1.5.12/linux-64/micromamba-1.5.12-0.tar.bz2
echo 'c8d0e74cdf31349b98af7a57b0faed1468c9ebb908ca95fc8fb6df2597e8a76f  /home/d508/.cache/codex-downloads/micromamba-1.5.12.tar.bz2' \
  | sha256sum -c -
tar -xjf /home/d508/.cache/codex-downloads/micromamba-1.5.12.tar.bz2 \
  -C /home/d508/.local/bin --strip-components=1 bin/micromamba
/home/d508/.local/bin/micromamba --version
```

- 影响范围：覆盖用户目录中当前不可用的micromamba二进制；不修改系统文件。
- 实际执行的关键命令：与计划命令一致，另外执行`micromamba --help | head -8`验证基本子命令解析。
- 实际结果：
  - 下载完成。
  - `sha256sum -c`返回`OK`，与官方元数据摘要一致。
  - `micromamba --version`输出`1.5.12`。
  - `micromamba --help`正常输出帮助信息。
- 结论：用户级环境管理工具恢复可用；不再使用会崩溃的2.9.0。
- 状态：完成。

### OP-019：查询Qt6、OpenCV和编译工具可用版本

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：conda-forge软件包索引查询
- 原因：创建环境前必须确认准确包名和版本，特别是Qt包名可能为`qt-main`或`qt6-main`。
- 计划执行的关键命令：

```bash
export MAMBA_ROOT_PREFIX=/home/d508/.local/share/mamba
micromamba search --override-channels -c conda-forge 'qt6-main=6.7.3'
micromamba search --override-channels -c conda-forge 'qt-main=6.7.3'
micromamba search --override-channels -c conda-forge 'opencv=4.10'
micromamba search --override-channels -c conda-forge 'gxx_linux-64'
```

- 影响范围：只下载和更新用户级包索引缓存，不创建环境。
- 实际执行的关键命令：与计划命令一致；命令设置120秒超时并对输出取尾部，避免网络异常无限等待和日志被完整依赖列表淹没。
- 实际结果：
  - `qt6-main=6.7.3`存在；其依赖包含XCB、Wayland、OpenSSL、libstdc++等Linux运行库。
  - `qt-main=6.7.3`不存在，因此Qt6.7.3的正确conda-forge包名是`qt6-main`。
  - `opencv=4.10`存在，但默认显示的是带Qt5和Python绑定的元包构建；直接安装该元包可能把Qt5引入Qt6环境。
  - `gxx_linux-64`存在，当前最新显示为16.1.0，并列出9.x、10.x等历史版本。
- 结论：环境应选`qt6-main`，OpenCV应优先选纯C++核心包`libopencv`而不是Python/Qt5元包；G++版本需继续显式查询和锁定。
- 状态：完成。

### OP-020：锁定编译器、OpenCV核心库和Qt Serial Port包

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：conda-forge软件包索引查询
- 原因：避免采用滚动最新版本，并确认Qt Serial Port是否已包含在Qt主包中或需要独立包。
- 计划执行的关键命令：

```bash
micromamba search --override-channels -c conda-forge 'gxx_linux-64=15.3'
micromamba search --override-channels -c conda-forge 'gxx_linux-64=15.2'
micromamba search --override-channels -c conda-forge 'libopencv=4.10.0'
micromamba search --override-channels -c conda-forge 'cmake=3.31.8'
micromamba search --override-channels -c conda-forge 'ninja=1.13.1'
micromamba search --override-channels -c conda-forge 'qt6-serialport'
```

- 影响范围：只读取已缓存或远端包索引，不创建环境。
- 实际执行的关键命令：与计划命令一致，每项查询设置60秒超时，并筛选名称、版本、构建和文件名字段。
- 实际结果：
  - `gxx_linux-64=15.3`存在，精确版本15.3.0。
  - `gxx_linux-64=15.2`也存在；为复现重装前环境，选择15.3.0。
  - `cmake=3.31.8`存在。
  - `ninja=1.13.1`存在。
  - `qt6-serialport`是独立包，当前最新为6.11.1；不能直接使用最新版本，必须继续查询与Qt主包一致的6.7.3。
  - `libopencv=4.10.0`默认结果仍为带Qt5/Python ABI的构建，需选择headless变体。
- 状态：完成。

### OP-021：确认Qt Serial Port 6.7.3和OpenCV headless构建，并进行环境求解模拟

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：包查询与环境创建模拟
- 原因：Qt模块必须保持相同小版本；OpenCV应避免引入Qt5；正式下载数百MB依赖前先用`--dry-run`验证依赖能够同时求解。
- 计划执行的关键命令：

```bash
micromamba search -c conda-forge 'qt6-serialport=6.7.3'
micromamba search -c conda-forge 'libopencv=4.10.0=*headless*'
micromamba create --dry-run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  -c conda-forge --override-channels \
  qt6-main=6.7.3 qt6-serialport=6.7.3 \
  'libopencv=4.10.0=*headless*' gxx_linux-64=15.3 \
  cmake=3.31.8 ninja=1.13.1 make pkg-config git
```

- 影响范围：只读取和缓存包索引；`--dry-run`不创建环境、不下载全部软件包。
- 实际执行的关键命令：与计划命令一致。
- 实际结果：
  - `qt6-serialport=6.7.3`不存在。
  - 指定Serial Port 6.7.3后，环境求解失败，错误明确指出该包不存在。
  - 因求解在Serial Port约束处终止，没有创建目标环境。
  - OpenCV headless查询返回了表头，但当前输出筛选没有保留具体构建行，需要重新输出完整结果确认。
- 结论：不能把`qt6-serialport=6.11.1`与Qt6.7.3混装。应先创建不含Serial Port的Qt6.7.3环境，再从Qt官方6.7.3源码构建该模块。
- 状态：完成，模拟失败但已定位原因。

### OP-022：重新求解Qt6.7.3环境并验证Qt Serial Port源码地址

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：包查询、环境求解模拟和下载地址验证
- 原因：排除不存在的conda包，确认其余依赖可以共存，并为后续源码构建准备准确来源。
- 计划执行的关键命令：

```bash
micromamba search -c conda-forge 'libopencv=4.10.0=*headless*'
micromamba create --dry-run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  -c conda-forge --override-channels \
  qt6-main=6.7.3 'libopencv=4.10.0=*headless*' \
  gxx_linux-64=15.3 cmake=3.31.8 ninja=1.13.1 make pkg-config git
wget -4 --spider -S \
  https://download.qt.io/official_releases/qt/6.7/6.7.3/submodules/qtserialport-everywhere-src-6.7.3.tar.xz
```

- 影响范围：只读取包索引和HTTP响应；不创建环境、不下载源码正文。
- 实际执行的关键命令：与计划命令一致。
- 实际结果：
  - OpenCV headless构建存在，搜索示例为`libopencv 4.10.0 headless_py38...`，共有多个历史构建。
  - 去掉不存在的Serial Port包后，环境求解成功。
  - 模拟方案包含200个包，总下载量约613 MB。
  - 关键锁定版本为Qt6.7.3、OpenCV4.10.0 headless、G++15.3.0、CMake3.31.8和Ninja1.13.1。
  - 模拟命令最终显示`Dry run. Not executing the transaction.`，确认尚未创建环境。
  - `official_releases/qt/6.7/6.7.3/...`源码地址返回HTTP 404。
- 解释：OpenCV选择headless变体，是因为工程只使用`cv::Mat`、图像处理和图像编解码，不依赖OpenCV HighGUI；这样避免引入Qt5并与项目Qt6插件冲突。
- 结论：环境规格可以求解；Qt Serial Port需要改用Qt归档目录地址。
- 状态：完成。

### OP-023：验证归档源码地址并创建x86_64 Qt6隔离环境

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：HTTP地址验证与用户级软件环境安装
- 原因：Qt6.7.3已经不是当前官方发布版，源码应位于`archive`；环境求解已经通过，可以执行实际下载。
- 计划执行的关键命令：

```bash
wget -4 --spider -S \
  https://download.qt.io/archive/qt/6.7/6.7.3/submodules/qtserialport-everywhere-src-6.7.3.tar.xz

export MAMBA_ROOT_PREFIX=/home/d508/.local/share/mamba
micromamba create -y \
  -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  --override-channels -c conda-forge \
  qt6-main=6.7.3 'libopencv=4.10.0=*headless*' \
  gxx_linux-64=15.3 cmake=3.31.8 ninja=1.13.1 \
  make pkg-config git
```

- 影响范围：在`/home/d508/.local/share/codex-envs/400w-qt-linux-v2`创建约613 MB下载量的用户级环境；不使用sudo，不修改`/usr`。
- 实际执行的关键命令：与计划命令一致。
- 实际结果：
  - Qt Serial Port归档URL返回HTTP 200，内容长度271,372字节。
  - micromamba事务实际安装200个包，下载量约613 MB。
  - 安装过程完成并输出`Transaction finished`。
  - 环境目录：`/home/d508/.local/share/codex-envs/400w-qt-linux-v2`。
- 说明：环境包含Qt、OpenCV、GCC工具链、CMake、Ninja、make、pkg-config和Git；所有文件位于用户目录，没有使用sudo。
- 状态：完成。

### OP-024：验证隔离环境工具版本、路径和模块完整性

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：只读环境验证
- 原因：安装事务成功不等于实际命令来自正确环境；必须核对路径、版本、OpenCV pkg-config和Qt Serial Port模块。
- 计划执行的关键命令：

```bash
micromamba run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 qmake -v
micromamba run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 g++ --version
micromamba run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 cmake --version
micromamba run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 ninja --version
micromamba run -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 pkg-config --modversion opencv4
find /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  -path '*QtSerialPort*' -o -name 'libQt6SerialPort.so*'
```

- 影响范围：只读，不修改环境或源码。
- 实际执行的关键命令：与计划命令一致，另外使用环境内Git检查虚拟机源码提交。
- 实际结果：
  - `cmake --version`为3.31.8。
  - `ninja --version`为1.13.1。
  - `pkg-config --modversion opencv4`为4.10.0，头文件和库路径均指向隔离环境。
  - 默认`PATH`中没有`qmake`和裸命令`g++`。
  - 没有找到Qt6SerialPort头文件或动态库，符合预期。
  - 虚拟机源码仍位于提交`226c87f`，比Windows本地日志提交旧，但业务源码树一致。
- 解释：conda的Linux编译器通常使用目标三元组前缀；Qt工具也可能位于`lib/qt6/bin`而不是环境顶层`bin`。需要查找真实路径，不能据此断言工具未安装。
- 状态：完成。

### OP-025：定位Qt构建工具和带前缀G++实际路径

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：只读文件和版本查询
- 原因：确定现有qmake工程应调用哪个工具，并为Qt Serial Port源码构建准备`qt-configure-module`。
- 计划执行的关键命令：

```bash
find /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  \( -name 'qmake*' -o -name 'qt-configure-module' -o -name 'qt-cmake*' \) -print
find /home/d508/.local/share/codex-envs/400w-qt-linux-v2/bin \
  -maxdepth 1 \( -name '*g++*' -o -name '*gcc*' \) -print
<找到的qmake路径> -v
<找到的G++路径> --version
```

- 影响范围：只读。
- 实际执行的关键命令：与计划命令一致；找到路径后分别直接执行版本命令。
- 实际结果：
  - 环境顶层存在`bin/qmake6`，Qt内部目录存在`lib/qt6/bin/qmake`和`qmake6`。
  - `qmake6 -v`输出QMake 3.1、Qt 6.7.3，库目录指向隔离环境。
  - 存在`lib/qt6/bin/qt-cmake`和`qt-configure-module`。
  - C++编译器为`bin/x86_64-conda-linux-gnu-g++`，版本15.3.0。
- 结论：主工程继续使用现有qmake工程文件，实际命令应写成`qmake6`；不需要为了工具路径问题重写CMakeLists.txt。
- 状态：完成。

### OP-026：下载、校验并解压Qt Serial Port 6.7.3源码

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：源码下载、完整性校验和解压
- 原因：conda-forge没有与Qt6.7.3匹配的Serial Port二进制包，必须使用同版本官方源码构建。
- 计划执行的关键命令：

```bash
wget -4 -O /home/d508/.cache/codex-downloads/qtserialport-everywhere-src-6.7.3.tar.xz \
  https://download.qt.io/archive/qt/6.7/6.7.3/submodules/qtserialport-everywhere-src-6.7.3.tar.xz
wget -4 -O /home/d508/.cache/codex-downloads/qtserialport-everywhere-src-6.7.3.tar.xz.sha256 \
  https://download.qt.io/archive/qt/6.7/6.7.3/submodules/qtserialport-everywhere-src-6.7.3.tar.xz.sha256
cd /home/d508/.cache/codex-downloads
sha256sum -c qtserialport-everywhere-src-6.7.3.tar.xz.sha256
mkdir -p /home/d508/.cache/codex-sources
tar -xJf qtserialport-everywhere-src-6.7.3.tar.xz \
  -C /home/d508/.cache/codex-sources
```

- 影响范围：只写入用户缓存和源码缓存目录；不修改Qt环境。
- 实际执行的关键命令：与计划命令一致。
- 实际结果：
  - 源码包下载成功，大小271,372字节。
  - 官方SHA-256文件内容为`d4fa58ee809b39c9eda8d20ee4677971e918edb9a076540466693bc46db146f0`。
  - `sha256sum -c`返回`OK`。
  - 源码解压到`/home/d508/.cache/codex-sources/qtserialport-everywhere-src-6.7.3`，已确认顶层CMakeLists和`src/`存在。
- 状态：完成。

### OP-027：配置Qt Serial Port 6.7.3独立构建目录

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：CMake/Qt模块配置
- 原因：先验证Qt安装前缀、编译器和依赖，再进入编译；配置失败比编译中途失败更容易定位。
- 计划执行的关键命令：

```bash
envp=/home/d508/.local/share/codex-envs/400w-qt-linux-v2
src=/home/d508/.cache/codex-sources/qtserialport-everywhere-src-6.7.3
build=/home/d508/.cache/codex-build/qtserialport-6.7.3
mkdir -p "$build"
cd "$build"
PATH="$envp/lib/qt6/bin:$envp/bin:$PATH" \
CC="$envp/bin/x86_64-conda-linux-gnu-gcc" \
CXX="$envp/bin/x86_64-conda-linux-gnu-g++" \
  "$envp/lib/qt6/bin/qt-configure-module" "$src" \
  -GNinja \
  -DCMAKE_INSTALL_PREFIX="$envp" \
  -DQT_BUILD_TESTS=OFF \
  -DQT_BUILD_EXAMPLES=OFF
```

- 影响范围：只生成用户缓存中的构建文件；尚不写入Qt环境。
- 实际执行的关键命令：与计划命令一致，并使用`tee configure.log`保存完整配置输出。
- 实际结果：失败，退出码127。
- 错误：`qt-configure-module`尝试执行`lib/qt6/bin/../lib/qt6/qt-cmake-private`，该相对路径不存在。
- 诊断：conda环境中的Qt包装脚本仍按另一种安装目录布局拼接内部路径；源码、编译器和CMake尚未真正进入配置阶段。
- 处理原则：不修改第三方环境里的包装脚本，改用等价的直接CMake配置并显式提供Qt前缀。
- 状态：失败，原因已定位。

### OP-028：绕过Qt包装脚本，使用CMake直接配置Serial Port

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：CMake配置
- 原因：`qt-configure-module`仅是CMake包装层；直接调用环境内CMake并传入Qt前缀可以避免错误相对路径。
- 计划执行的关键命令：

```bash
envp=/home/d508/.local/share/codex-envs/400w-qt-linux-v2
src=/home/d508/.cache/codex-sources/qtserialport-everywhere-src-6.7.3
build=/home/d508/.cache/codex-build/qtserialport-6.7.3
PATH="$envp/bin:$PATH" \
CC="$envp/bin/x86_64-conda-linux-gnu-gcc" \
CXX="$envp/bin/x86_64-conda-linux-gnu-g++" \
  "$envp/bin/cmake" -S "$src" -B "$build" -GNinja \
  -DCMAKE_PREFIX_PATH="$envp" \
  -DCMAKE_INSTALL_PREFIX="$envp" \
  -DQT_BUILD_TESTS=OFF \
  -DQT_BUILD_EXAMPLES=OFF
```

- 影响范围：生成或更新用户缓存中的构建文件；尚不执行安装。
- 实际执行的关键命令：与计划命令一致，并使用`tee configure-cmake.log`保存完整输出。
- 实际结果：成功，退出码0。
  - C/C++编译器均识别为GNU 15.3.0。
  - CMake找到Threads和原子操作支持。
  - Qt Gui/Widgets的可选检查报告缺少WrapOpenGL，但Serial Port核心模块不依赖Gui，配置未因此失败。
  - 生成目录：`/home/d508/.cache/codex-build/qtserialport-6.7.3`。
- 风险记录：主GUI工程依赖QOpenGLWidget，后续必须补齐OpenGL开发文件；本次仅允许Serial Port继续编译。
- 状态：完成。

### OP-029：编译并安装Qt Serial Port 6.7.3

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：源码编译、安装和版本验证
- 原因：补齐主工程声明的`QT += serialport`模块，且版本必须与Qt6.7.3一致。
- 计划执行的关键命令：

```bash
envp=/home/d508/.local/share/codex-envs/400w-qt-linux-v2
build=/home/d508/.cache/codex-build/qtserialport-6.7.3
"$envp/bin/cmake" --build "$build" --parallel 4
"$envp/bin/cmake" --install "$build"
find "$envp/include/qt6/QtSerialPort" -maxdepth 1 -type f
ls -l "$envp/lib/libQt6SerialPort.so"*
grep -R 'set(PACKAGE_VERSION' "$envp/lib/cmake/Qt6SerialPort"
```

- 影响范围：向用户级Qt环境写入Qt6SerialPort头文件、动态库、CMake元数据和pkg-config文件；不修改系统目录。
- 实际执行的关键命令：与计划命令一致，编译与安装输出分别保存为`build.log`和`install.log`。
- 实际结果：
  - Ninja共执行16个步骤，全部成功。
  - 生成`libQt6SerialPort.so.6.7.3`及对应符号链接。
  - 构建退出码0，安装退出码0。
  - 已安装`QSerialPort`、`QSerialPortInfo`等公开头文件。
  - 已安装CMake、qmake和pkg-config元数据。
  - CMake版本文件明确记录`PACKAGE_VERSION 6.7.3`。
- 状态：完成。

### OP-030：补齐OpenGL开发头并冻结现有环境版本

- 日期：2026-08-16
- 环境：Ubuntu虚拟机
- 类型：包查询、求解模拟和用户级环境增量安装
- 原因：Qt Serial Port配置已暴露WrapOpenGL缺失；主工程使用QOpenGLWidget，必须提供`GL/gl.h`等开发文件。
- 计划执行的关键命令：

```bash
micromamba search -c conda-forge 'libgl-devel=1.7.0'
micromamba install --dry-run --freeze-installed \
  -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  -c conda-forge --override-channels libgl-devel=1.7.0
micromamba install -y --freeze-installed \
  -p /home/d508/.local/share/codex-envs/400w-qt-linux-v2 \
  -c conda-forge --override-channels libgl-devel=1.7.0
test -f /home/d508/.local/share/codex-envs/400w-qt-linux-v2/include/GL/gl.h
```

- 解释：`--freeze-installed`要求求解器保持已经安装的Qt6.7.3、OpenCV4.10和编译器版本不变，只增加缺失包。
- 影响范围：只修改用户级隔离环境，不使用sudo。
- 实际执行的关键命令：与计划命令一致，安装后增加CMake的OpenGL查找验证。
- 实际结果：
  - `libgl-devel=1.7.0`存在。
  - dry-run显示只新增`xorg-xorgproto`、`libglx-devel`和`libgl-devel`三个包，总下载738 KB。
  - 正式安装事务完成，没有升级Qt、OpenCV或编译器。
  - `include/GL/gl.h`存在。
  - CMake包查找输出`OpenGL found.`。
- 状态：完成。

### OP-031：提交并同步迁移日志到Ubuntu虚拟机

- 日期：2026-08-16
- 环境：Windows主机与Ubuntu虚拟机
- 类型：Git提交、bundle传输和快进合并
- 原因：第一次主工程构建前，使两个环境的源码提交与执行日志保持一致；虚拟机没有直接访问Windows文件系统。
- 计划执行的关键命令：

```powershell
git -C D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux diff --check
git -C D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux add -- docs/迁移执行日志.md
git -C D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux commit \
  -m '记录Ubuntu Qt6环境重建与依赖验证'
git -C D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux bundle create \
  work/2025_SWIRVision_Linux.bundle main
scp work/2025_SWIRVision_Linux.bundle ubuntu20-vm:/home/d508/projects/
```

```bash
git -C /home/d508/projects/2025_SWIRVision_Linux fetch \
  /home/d508/projects/2025_SWIRVision_Linux.bundle main
git -C /home/d508/projects/2025_SWIRVision_Linux merge --ff-only FETCH_HEAD
```

- 解释：`--ff-only`只允许线性快进；如果虚拟机有独立提交或分叉会立即失败，不会自动生成合并或覆盖修改。
- 影响范围：创建一个本地文档提交和临时bundle文件；不推送原远端。
- 状态：等待执行并补充结果。
