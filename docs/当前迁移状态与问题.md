# 400W Qt Linux 迁移当前状态与问题

- 整理日期：2026-08-16
- 当前状态：RK3588 已恢复 SSH 连接；已完成第一轮只读环境盘点，尚未在板端安装软件或部署程序。
- 当前重点：保持 Qt 6 技术路线，先解决板端 Qt 6 部署方案与 Rockchip GPU 硬件加速问题，再开始 ARM64 构建。

## 一、当前结论

1. Linux 版本已经改为独立项目，不在现有 Windows 项目中继续开发。
2. 独立项目保留原仓库远端 `originlab`；Linux `main` 已修复为从 Windows 基线直接派生，可以正常使用提交历史、共同祖先和逐提交差异。
3. Qt 技术路线保持 Qt 6，不再评估 Qt 5 兼容构建；最终 Qt 6 小版本等待 RK3588 环境核验后锁定。
4. x86_64 Linux 已进入真实编译阶段，但尚未完成一次全量编译，更没有完成 GUI、OpenGL、离线回放或真机 USB 验证。
5. Linux USB 后端当前只是明确拒绝设备打开的占位实现，尚未接入 libusb，不能视为 USB 迁移完成。

## 二、源码与 Git 基线

### Windows 原项目

- 路径：`D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision`
- 分支：`master`
- 当前提交：`d4c0131ad3c1c81ad9090396efb223befc278dd8`
- 提交说明：`完善串口两点采集使用的积分时间自动计算和USB解析逻辑`
- 当前状态：工作区干净，`master` 与 `originlab/master` 一致。
- 最初发现的 4 个未提交文件已经进入上述提交，没有丢失：
  - `readme.md`
  - `telemetrydebugdialog.cpp`
  - `telemetrydebugdialog.h`
  - `transfer_thread.cpp`

### Linux 独立项目

- 路径：`D:\hjx_workspace\Prj\SWIR_400W\Prj_Qt\2025_SWIRVision_Linux`
- 分支：`main`
- 当前提交：`0daec20 建立400W Linux独立项目基线（继承Windows Qt6基线）`
- 直接父提交：`d4c0131`，即 Windows 当前基线。
- 修复前的无父提交版本已保留为备份分支：`backup/pre-history-fix-20260816`，提交为 `90ce072`。
- 远端：保留 `originlab = ssh://git@122.205.5.3:22131/D508/2025_SWIRVision.git`
- 对比命令：`git diff originlab/master main`
- 共同祖先验证：`git merge-base main originlab/master` 返回 `d4c0131ad3c1c81ad9090396efb223befc278dd8`。
- 修复前后代码树均为 `0df88a8c93fcbbe49c3f7787097bdcead354c5f6`，说明只修复了提交继承关系，没有改变源码内容。
- 当前相对 Windows 基线的代码差异：11 个文件，243 行新增、57 行删除。

## 三、已经完成的审计

- 确认工程使用 qmake，主构建文件为 `SWIRVision.pro`。
- 确认 Windows 历史 Kit 为 Qt 6.4.2/MSVC2019，当前 Kit 为 Qt 6.10.0/MSVC2022；本机另有 Qt 6.7.3。
- 确认工程真实依赖 OpenCV，图像处理、两点校正、坏点处理和图像保存大量使用 `cv::Mat`。
- 确认源码使用 `std::clamp`，构建标准应从声明的 C++11 提升到 C++17。
- 确认 GUI、OpenGL 图像显示、Qt Serial Port 和多数文件处理代码具备跨平台基础。
- 确认 WinUSB、设备枚举、端点描述、`HANDLE`、`OVERLAPPED` 和四通道异步传输与 Windows API 深度耦合。
- 确认当前 400W 工程没有可直接使用的原始图像离线回放入口，需要新增独立、明确标记的离线数据源。
- EVS 仅完成只读基线审计，未开始迁移，符合“先完成 400W”的顺序要求。

## 四、Linux 独立项目当前修改

- 将 Windows 链接库和 MSVC 栈参数限制在 Windows 构建分支。
- 将 OpenCV Linux 链接改为 `pkg-config/opencv4`。
- 将 C++ 标准提升到 C++17。
- 新增平台无关的 USB 端点类型 `common/usb_types.h`。
- 主窗口不再直接使用 `WINUSB_PIPE_INFORMATION_EX`。
- Windows 后端仍保留原 WinUSB 实现。
- 新增 Linux 设备枚举、设备对象和传输线程占位实现；占位实现会明确报告“libusb 后端尚未实现”，不会伪装成真机采集成功。

涉及文件：

- `SWIRVision.pro`
- `common/device.h`
- `common/device_linux.cpp`
- `common/tih_usb_device.cpp`
- `common/tih_usb_device.h`
- `common/tih_usb_device_linux.cpp`
- `common/usb_types.h`
- `mainwindow.cpp`
- `transfer_thread.cpp`
- `transfer_thread.h`
- `transfer_thread_linux.cpp`

## 五、环境与构建状态

### Ubuntu 虚拟机

- SSH 别名：`ubuntu20-vm`
- 系统：Ubuntu 20.04.6 LTS
- 架构：x86_64
- 内核：5.15.0-139-generic
- 系统初始状态：没有 GCC、CMake、qmake、Qt、OpenCV、glxinfo。
- 当前 SSH 会话：`DISPLAY` 为空，不能据此完成真实 GUI/OpenGL 显示验证。
- sudo：账户属于 sudo 组，但非交互 sudo 需要密码，因此采用用户级隔离工具链，没有替换系统库。

### 用户级隔离环境

有效运行环境：

- 路径：`/home/d508/.local/share/codex-envs/400w-qt-linux-v2`
- Qt：6.7.3
- OpenCV：4.10.0
- GCC：15.3.0
- Qt Serial Port：6.7.3，由 Qt 官方子模块源码构建并安装
- OpenGL 开发头：已补装 `libgl-devel 1.7.0`

独立构建工具环境：

- 路径：`/home/d508/.local/share/codex-envs/400w-build-tools`
- CMake：3.31.8
- Ninja：1.13.2

废弃但尚未删除的环境：

- `/home/d508/.local/share/codex-envs/400w-qt-linux`
- 原因：安装 CMake 时求解器试图连带升级 Qt/OpenCV，中止事务后环境不完整。
- 当前处理：不再使用；为避免未经确认的删除，暂时保留。

### Ubuntu 源码与构建目录

- 源码：`/home/d508/projects/2025_SWIRVision_Linux`
- 构建目录：`/home/d508/projects/2025_SWIRVision_Linux/build/x86_64-release`
- qmake 日志：`qmake.log`
- 编译日志：`build.log`

### RK3588 开发板（2026-08-16 实测）

- SSH 别名：`tl3588`，连接正常。
- 系统：Debian 11，AArch64，内核 `5.10.160-rt77-gfbf758505158`。
- 桌面：LightDM + Xorg `:0` + Xfce，当前桌面总尺寸为 `1824×1280`。
- 显示接口：HDMI 和 DP 未连接；DSI-1 已连接，分辨率 `800×1280`；DSI-2 已连接，分辨率 `1024×768`。
- 输入设备：已识别 ADS7846 触摸屏和 Logitech USB 鼠标/键盘接收器，可直接操作本地 Qt 界面。
- Qt：发现 Qt 5.15.8 厂商运行库和 Qt 5.15.2 Debian 运行库，发现 `xcb`、`eglfs`、`linuxfb`、`offscreen` 等平台插件；未发现 Qt 6 或可直接使用的 qmake。
- OpenGL：X11 下 Rockchip DRI 驱动加载失败，当前渲染器为 Mesa `llvmpipe`，属于 CPU 软件渲染，不能作为最终性能方案。
- libusb：存在 AArch64 的 `libusb-1.0.so.0` 运行库，但尚未确认开发头文件和 pkg-config 开发包。
- USB：当前 `lsusb` 未发现 T630 目标设备，尚不能进行端点、速率和权限验证。

### RK3588 本地显示与控制方案

- 本板已经有两路 DSI 显示输出，因此本地可视化不要求另接 HDMI；HDMI 只是一种可选显示输出。
- HDMI/DSI 只负责输出画面，功能控制由触摸屏、USB 鼠标键盘或远程控制接口完成。
- 第一阶段沿用 Qt 主界面的连接、开始和停止按钮，通过现有触摸屏或鼠标操作。
- 程序生命周期与采集状态必须分开：systemd/桌面入口负责启动应用，Qt 按钮负责打开设备、开始传输和停止传输。
- 后续将采集控制整理为“未连接—已连接—传输中—停止中—已连接/错误”状态机，并为远程调试预留本地 Socket 或命令行控制入口。
- USB 采集、帧重组和显示刷新必须解耦；显示端只获取最新完整帧，不能因屏幕刷新率或界面阻塞采集链路。

## 六、已遇到的问题

| 问题 | 当前判断 | 当前状态 |
| --- | --- | --- |
| 原工程无条件链接 SetupAPI/user32/cfgmgr32/winusb | 构建文件平台条件缺失 | 已在 Linux 独立项目中隔离 |
| Linux 分支仍引用 Windows OpenCV 路径 | 原 `.pro` 的 Unix 分支配置错误 | 已改为 `pkg-config opencv4` |
| 源码声明 C++11 但使用 `std::clamp` | GCC 下需要 C++17 | 已调整 |
| 公共接口暴露 WinUSB 类型 | 主窗口和传输线程无法在 Linux 编译 | 已完成第一层公共端点类型解耦 |
| Ubuntu 没有系统编译工具和 Qt | 不能直接构建 | 已建立用户级隔离环境 |
| Qt 6.7.3 环境缺少 Serial Port 模块 | conda-forge 没有可靠的匹配包 | 已从 Qt 官方 6.7.3 源码构建 |
| 首次 qmake 报 `Unknown module(s) in QT: serialport` | Serial Port 模块缺失 | 已解决，尚未重跑最终全量构建 |
| 第二次编译报 `GL/gl.h: No such file or directory` | OpenGL 开发头缺失 | 已补装，尚未重跑编译 |
| SSH 会话没有 `DISPLAY` | 不能进行真实窗口和 OpenGL 上下文验证 | 待确定虚拟机桌面显示方式 |
| Linux USB 后端未实现 | 需要 libusb/usbfs 或厂商 SDK | 待后续设计与真机验证 |
| 400W 没有离线回放入口 | GUI—解析—显示链路缺少无设备测试源 | 待新增 |
| RK3588 SSH 超时 | 地址为 192.168.1.114 时曾无法连接 | 已恢复，SSH 别名 `tl3588` 可用 |
| RK3588 未安装 Qt 6 | 板端仅发现 Qt 5 运行库，且 Qt 5.15.8/5.15.2 并存 | 待确定 Qt 6 交叉部署或板端安装方案 |
| RK3588 OpenGL 使用 llvmpipe | Rockchip DRI 驱动加载失败，当前为 CPU 软件渲染 | 必须先修复硬件加速再做显示性能验收 |
| T630 未在 RK3588 枚举 | 当前 `lsusb` 中没有目标设备 | 待连接 T630 后检查 VID/PID、端点和 SuperSpeed 链路 |

## 七、尚未验证的内容

以下内容当前均不能标记为完成：

- x86_64 Linux 全量编译通过。
- Linux GUI 启动。
- OpenGL 3.3 上下文建立和硬件加速。
- 原始图像离线回放。
- 图像处理、保存、配置和日志回归。
- Linux 串口真机收发。
- libusb 设备枚举和四通道异步 Bulk IN。
- USB 取消、超时、热插拔和异常恢复。
- RK3588 ARM64 编译与部署。
- 400W Linux/RK3588 吞吐、CPU、内存、丢行、丢帧和稳定性指标。
- Windows Qt 6 回归构建。

## 八、下一步

1. 补充检查 RK3588 的编译器、存储空间、Qt 5 厂商库来源以及 Mali/DRM/EGL 驱动链，定位 `llvmpipe` 原因。
2. 在“不替换系统 Qt 5”的前提下确定 Qt 6 独立部署方式，并锁定与 x86_64 一致的小版本。
3. 连接 T630 后检查 VID/PID、四个 Bulk IN 端点、SuperSpeed 协商结果、udev 权限和 libusb 开发环境。
4. 返回 x86_64 环境完成全量编译闭环，再开始 RK3588 ARM64 构建与本地 DSI 显示验证。
